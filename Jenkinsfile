pipeline {
    agent any
    stages {
        stage('Check Tools') {
            steps {
                echo 'Checking if everything is installed...'
            }
        }
        stage('Finish') {
            steps {
                echo 'GitHub and Jenkins are connected!'
            }
        }
    }
}
